# 在线写作后台使用说明

写完这篇文章，你就能在浏览器里写文章、点一下发布，不用再碰 VS Code 和 Git。

后台地址：

```text
https://molt213.top/admin
```

---

## 一、先做一次性配置（大约 10 分钟）

配置分三件事：给后台一把“改仓库的钥匙”、把钥匙交给 Cloudflare、给后台装一道门。

### 第 1 步：在 GitHub 申请一把钥匙（Token）

1. 打开 <https://github.com/settings/personal-access-tokens/new>（也可以在 GitHub 里点右上角头像 → Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token）。
2. **Token name** 填 `blog-admin`。
3. **Expiration** 选 90 days（也可以选更长，但记住到期要换，见最后一节）。
4. **Repository access** 选 `Only select repositories`，然后只勾选 `personal_blog`。
5. 往下找到 **Permissions** → **Repository permissions**，把 **Contents** 设成 **Read and write**（其他都保持 No access）。
6. 点最下面的 **Generate token**，页面会显示一串以 `github_pat_` 开头的字符。
7. **立刻复制它**——离开这个页面就再也看不到了。

> 这串字符等于你仓库的钥匙。不要发给任何人，不要写进任何文件或代码，也不要粘贴给 AI 助手。

### 第 2 步：把钥匙交给 Cloudflare

1. 打开 Cloudflare 面板 → 左侧 **Workers & Pages** → 点进 `personal-blog`。
2. 上方选 **Settings** → 找到 **Variables and Secrets** → 点 **Add**。
3. 类型选 **Secret**（重要，不要选 Text），名字填 `GITHUB_TOKEN`，值粘贴刚才那串 `github_pat_…`。
4. 保存。Cloudflare 会提示需要重新部署，按提示部署一次即可。

### 第 3 步：给后台装一道门（Cloudflare Access）

> 注意：Cloudflare 已经把这块界面改版了，下面按新版叫法写。老版把「访问控制」直接叫 Access、把「自托管和专用」叫 Self-hosted。

1. Cloudflare 面板左侧找到 **Zero Trust**（现在叫 Cloudflare One）。第一次进入会让你起一个团队名（team name），随便填一个英文名，免费版够用。
2. 进 **访问控制**（Access controls）→ **应用程序**（Applications）→ **创建新应用** → 选 **自托管和专用**。
3. **公共主机名** 点「添加公共主机名」：域名选 `molt213.top`，路径填 `admin`。
4. 再加一条：域名还是 `molt213.top`，路径填 `api/admin`。
   （如果界面上加不了第二条，就先把这条保存，再重复第 2–6 步建第二个应用，路径填 `api/admin`。路径只写 `admin`，不要写 `/admin`，也不要写整个域名。）
5. **Access 策略** 里新建一条：策略名称填 `站长`，操作选 **允许**，选择器选 **电子邮件**，值填 `wurui640@gmail.com`。
6. **身份验证**：保持「接受所有可用的标识提供程序」开着即可，下面几个开关都不用动。
7. **会话持续时间** 保持默认 24 小时，点 **创建**。

### 第 4 步：试试看

浏览器打开 <https://molt213.top/admin>：

- 正常情况：先跳到 Cloudflare 的登录页 → 用 Cloudflare 账号登录（Sign in with Cloudflare）→ 进入写作后台。
- 想用邮箱验证码登录的话，先去 **设置 → 身份验证 → 登录方式** 里添加 **One-time PIN**，登录页上才会出现邮箱输入框。
- 如果提示 **That account does not have access**：登录用的邮箱不在 Access 策略里。登录邮箱、策略里的邮箱、`wrangler.jsonc` 里的 `ADMIN_EMAIL` 三处必须一致。
- 如果看到 403 页面：Access 那一步还没生效，检查第 3 步的域名和路径有没有写对。

---

## 二、怎么写一篇文章

1. 打开 <https://molt213.top/admin>，点左上角的 **＋ 新建文章**。
2. 填 **标题**，然后把 **网址代号** 改成英文（例如 `kanc-2026-09`）。代号会出现在文章网址里，只能用英文小写、数字和短横线。
   - 网址代号建好之后不能再改，改了旧链接就失效了。
3. 填 **摘要**（列表里显示的一句话）和 **导语**（文章页标题下面那句，留空就和摘要一样）。
4. **卡片标签**：首页文章卡片上那几行大字，可以按回车换行。没有封面图时就显示它。
5. **封面地址**：留空即可，后台会自动取正文里的第一张图片当封面。想指定别的图，就填 `/images/xxx.png` 或图片网址。
6. 在 **正文** 里写内容。可以直接写 HTML，也可以点工具栏上的按钮插入常用片段：
   - 上传图片、外链图片、图片带说明、大标题、小标题、段落、灰字注释、引用框、链接、视频、音乐、剧透块。
   - 点一下按钮，代码就会插到光标位置，把里面的示例文字换成你自己的即可。
   - **先选中一段文字再点按钮**，那段文字会被直接包进去（例如选中「重点」点灰字注释，会变成 `<small class="note">重点</small>`）。
7. **插图有三种办法**，都不用离开后台：
   - 把图片文件**直接拖进**编辑区（拖进去时编辑区会高亮）
   - 用截图工具截好图后，在正文里按 **Ctrl + V** 粘贴
   - 点工具栏的 **上传图片**，一次可以选多张
   图片会自动存进仓库的 `public/images/`，并在光标处插入 `<img>` 标签。单张限制 5 MB，支持 png / jpg / webp / gif。
8. 右边（手机上是下面）会实时显示 **预览**，看到的就是文章最终的样子。
9. 满意后点 **发布　↗**，确认一次，就提交到 GitHub 了。**大约一分钟后**刷新首页就能看到。

快捷键：**Ctrl + S** 存草稿，**Ctrl + Enter** 发布。

其他按钮：

| 按钮 | 作用 |
| --- | --- |
| 存草稿 | 把当前内容存到云端草稿箱，只有你能看到，不会提交到 GitHub |
| 恢复草稿 | 这篇有草稿时出现，点一下把草稿内容填回表单 |
| 刷新预览 | 手动刷新右侧预览 |
| 删除这篇 | 从仓库里删掉这篇（正文和资料一起删），**不可撤销** |

> 后台每 45 秒会自动存一次草稿，所以写一半关掉网页也不会丢。草稿只有你自己能看到，不会出现在网站上。

---

## 三、遇到问题怎么办

**发布失败，提示 GitHub 返回 401 / 403**
Token 失效或过期了。重新做第 1 步生成新 Token，再做第 2 步把它换进 Cloudflare（Secret 名字还是 `GITHUB_TOKEN`）。

**提示“仓库在你编辑期间有了新提交，请刷新后重试”**
说明这篇文章在你编辑的这段时间里被别处改过（比如你手动改了文件，或者另一个 AI 改了仓库）。刷新页面重新打开这篇，再改一次就好——这样不会覆盖掉别人的修改。

**点发布成功了，但网站上看不到变化**
正常要等约一分钟。超过 5 分钟还没变，去 Cloudflare 面板 → `personal-blog` → **Deployments**，看最新一次构建是不是失败了（构建失败时网站会保持旧版本）。

**后台页面直接显示 403**
说明请求没经过 Cloudflare Access，或者你的登录邮箱和配置里的 `ADMIN_EMAIL` 不一致。检查第 3 步。

**“后台还没有配置 GitHub 密钥”的黄色提示**
第 2 步还没做完，或者 Secret 名字拼错了（必须是 `GITHUB_TOKEN`）。

---

## 四、想在本机先试试（不影响线上）

在项目目录里运行：

```powershell
npx wrangler dev
```

然后打开 <http://127.0.0.1:8787/admin>。本机预览不需要登录，但发布功能需要在本机也放一份钥匙：

1. 在项目根目录新建一个文件 `.dev.vars`（这个文件已经在 `.gitignore` 里，不会进仓库）。
2. 写一行：

   ```text
   GITHUB_TOKEN=你的token
   GITHUB_BRANCH=admin-test
   ```

   `GITHUB_BRANCH=admin-test` 的意思是：本机测试时提交到一个测试分支，不会动 `main`、不会影响线上。测完把测试分支删掉即可。

---

## 五、后台是怎么工作的（好奇的话看看）

```text
你在 /admin 写文章
      ↓
Cloudflare Access 先检查身份（邮箱验证码）
      ↓
Worker 拿着 GITHUB_TOKEN 调 GitHub API，提交一次 commit
      ↓
GitHub 的 main 分支更新
      ↓
Cloudflare 自动构建部署（约 1 分钟）
      ↓
线上更新
```

文章内容和以前一样，**全部存在 GitHub 仓库里**：

- 文章资料：`content/posts/posts.json`
- 文章正文：`public/posts/<网址代号>.html`

所以后台只是“替你写文件并提交”的工具。你、或者别的 AI，依然可以直接在仓库里改文章，两边不冲突。